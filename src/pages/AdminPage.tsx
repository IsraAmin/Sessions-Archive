import { useEffect, useMemo, useState, type FormEvent, type ReactNode } from 'react'
import { publicStorageUrl, supabase } from '../lib/supabase'
import { StatCard } from '../components/StatCard'
import type { Category, Profile, Session, SessionSeries, SessionStatus, SessionVideo, Speaker } from '../types/domain'
import { errorMessage } from '../lib/errors'
import { extractYouTubeVideoId } from '../lib/youtube'
import { YouTubePlayer } from '../components/YouTubePlayer'
import { useUi } from '../hooks/useUi'
import { useAuth } from '../hooks/useAuth'
import { useToast } from '../components/ToastProvider'
import { Icon } from '../components/Icon'
import { StarRating } from '../components/StarRating'
import { ConfirmDialog } from '../components/ConfirmDialog'
import { AdminEditorDialog, type EditTarget } from '../components/AdminEditorDialog'

type RegistrationAnalytics = { user_id: string; session_id: string; attendance_status: string; session: { category_id: string | null; speaker_id: string | null } | null }
type FeedbackAnalytics = { user_id: string; session_id: string; rating: number; session: { category_id: string | null; speaker_id: string | null } | null }
type ViewAnalytics = { user_id: string; session_id: string }
type ProgressAnalytics = { user_id: string; video_id: string; video: { session_id: string } | null }
type ManagedUser = {
  id: string; email: string; phone: string | null; full_name: string; role: 'admin' | 'student'; super_admin: boolean
  banned_until: string | null; created_at: string; updated_at: string | null; last_sign_in_at: string | null
  email_confirmed_at: string | null; phone_confirmed_at: string | null; is_anonymous: boolean; providers: string[]
  profile: Profile | null; activity: { registrations: number; feedback: number; video_progress: number }
}
type Confirmation = { title: string; description: string; confirmLabel: string; tone: 'danger' | 'warning' | 'primary'; action: () => Promise<void> } | null

function FormField({ label, children, wide = false, hint }: { label: string; children: ReactNode; wide?: boolean; hint?: string }) {
  return <label className={`form-field ${wide ? 'wide' : ''}`}><span className="field-label">{label}</span>{children}{hint && <span className="field-hint">{hint}</span>}</label>
}
function rankedEntries(values: Map<string, number>, labels: Map<string, string>, limit = 5) {
  return [...values.entries()].map(([id, value]) => ({ id, value, label: labels.get(id) ?? id })).sort((a, b) => b.value - a.value).slice(0, limit)
}

export function AdminPage() {
  const { language, locale, t } = useUi(); const ar = language === 'ar'
  const { isSuperAdmin } = useAuth(); const { showToast } = useToast()
  const [categories, setCategories] = useState<Category[]>([]); const [speakers, setSpeakers] = useState<Speaker[]>([])
  const [sessions, setSessions] = useState<Session[]>([]); const [series, setSeries] = useState<SessionSeries[]>([]); const [videos, setVideos] = useState<SessionVideo[]>([])
  const [profiles, setProfiles] = useState<Profile[]>([]); const [registrations, setRegistrations] = useState<RegistrationAnalytics[]>([])
  const [feedbackRows, setFeedbackRows] = useState<FeedbackAnalytics[]>([]); const [viewRows, setViewRows] = useState<ViewAnalytics[]>([]); const [progressRows, setProgressRows] = useState<ProgressAnalytics[]>([])
  const [managedUsers, setManagedUsers] = useState<ManagedUser[]>([]); const [userSearch, setUserSearch] = useState(''); const [selectedUserId, setSelectedUserId] = useState<string | null>(null)
  const [videoSessionId, setVideoSessionId] = useState(''); const [videoTitle, setVideoTitle] = useState(''); const [videoUrl, setVideoUrl] = useState('')
  const [busyUsers, setBusyUsers] = useState(false); const [editing, setEditing] = useState<EditTarget | null>(null); const [editorBusy, setEditorBusy] = useState(false)
  const [confirmation, setConfirmation] = useState<Confirmation>(null); const [confirmBusy, setConfirmBusy] = useState(false)

  function success(message: string) { showToast({ kind: 'success', title: t('common.success'), message }) }
  function fail(error: unknown) { showToast({ kind: 'error', title: t('common.error'), message: errorMessage(error) }) }

  async function load() {
    const [cat, spk, ses, ser, vid, prof, regs, feeds, views, progress] = await Promise.all([
      supabase.from('categories').select('*').order('name'), supabase.from('speakers').select('*').order('name'),
      supabase.from('sessions').select('*').order('starts_at', { ascending: false }), supabase.from('session_series').select('*').order('created_at', { ascending: false }),
      supabase.from('session_videos').select('*').order('session_id').order('position'), supabase.from('profiles').select('*'),
      supabase.from('registrations').select('user_id,session_id,attendance_status,session:sessions(category_id,speaker_id)'),
      supabase.from('feedback').select('user_id,session_id,rating,session:sessions(category_id,speaker_id)'), supabase.from('session_views').select('user_id,session_id'),
      supabase.from('video_progress').select('user_id,video_id,video:session_videos(session_id)'),
    ])
    setCategories((cat.data ?? []) as Category[]); setSpeakers((spk.data ?? []) as Speaker[]); setSessions((ses.data ?? []) as Session[]); setSeries((ser.data ?? []) as SessionSeries[])
    setVideos((vid.data ?? []) as SessionVideo[]); setProfiles((prof.data ?? []) as Profile[]); setRegistrations((regs.data ?? []) as unknown as RegistrationAnalytics[])
    setFeedbackRows((feeds.data ?? []) as unknown as FeedbackAnalytics[]); setViewRows((views.data ?? []) as ViewAnalytics[]); setProgressRows((progress.data ?? []) as unknown as ProgressAnalytics[])
  }
  async function loadManagedUsers() {
    if (!isSuperAdmin) return
    setBusyUsers(true)
    try { const { data, error } = await supabase.functions.invoke('manage-users', { body: { action: 'list' } }); if (error) throw error; const payload = data as { users?: ManagedUser[]; error?: string }; if (payload.error) throw new Error(payload.error); setManagedUsers(payload.users ?? []) }
    catch (error) { fail(error) } finally { setBusyUsers(false) }
  }
  useEffect(() => { void load() }, [])
  useEffect(() => { if (isSuperAdmin) void loadManagedUsers() }, [isSuperAdmin])

  async function run(task: () => PromiseLike<{ error: unknown }>, successMessage: string) {
    const result = await task(); if (result.error) throw result.error; success(successMessage); await load()
  }
  function ask(action: Exclude<Confirmation, null>) { setConfirmation(action) }
  async function executeConfirmation() {
    if (!confirmation) return
    setConfirmBusy(true)
    try { await confirmation.action(); setConfirmation(null) } catch (error) { fail(error) } finally { setConfirmBusy(false) }
  }

  async function addCategory(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const el = event.currentTarget; const f = new FormData(el); const name = String(f.get('name') || '').trim(); const slug = String(f.get('slug') || '').trim(); if (!name || !slug) return; try { await run(() => supabase.from('categories').insert({ name, slug, description: String(f.get('description') || '').trim() || null }), ar ? 'تمت إضافة التصنيف.' : 'Category added.'); el.reset() } catch (e) { fail(e) } }
  async function addSpeaker(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const el = event.currentTarget; const f = new FormData(el); const name = String(f.get('name') || '').trim(); if (!name) return; try { await run(() => supabase.from('speakers').insert({ name, organization: String(f.get('organization') || '').trim() || null, bio: String(f.get('bio') || '').trim() || null }), ar ? 'تمت إضافة المتحدث.' : 'Speaker added.'); el.reset() } catch (e) { fail(e) } }
  async function addSeries(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const el = event.currentTarget; const f = new FormData(el); const title = String(f.get('title') || '').trim(); if (!title) return; try { await run(() => supabase.from('session_series').insert({ title, description: String(f.get('description') || '').trim() || null, published: true }), ar ? 'تم إنشاء السلسلة.' : 'Series created.'); el.reset() } catch (e) { fail(e) } }
  async function addSession(event: FormEvent<HTMLFormElement>) {
    event.preventDefault(); const el = event.currentTarget; const f = new FormData(el); const seriesId = String(f.get('series_id') || '') || null; const status = String(f.get('status') || 'published') as SessionStatus
    const payload = { title: String(f.get('title') || '').trim(), slug: String(f.get('slug') || '').trim(), description: String(f.get('description') || '').trim(), starts_at: new Date(String(f.get('starts_at'))).toISOString(), ends_at: String(f.get('ends_at') || '') ? new Date(String(f.get('ends_at'))).toISOString() : null, location: String(f.get('location') || '').trim() || null, capacity: Number(f.get('capacity') || 30), category_id: String(f.get('category_id') || '') || null, speaker_id: String(f.get('speaker_id') || '') || null, series_id: seriesId, series_position: seriesId ? Number(f.get('series_position') || 1) : null, status }
    if (!payload.title || !payload.slug || !payload.description) return
    try { await run(() => supabase.from('sessions').insert(payload), ar ? 'تم إنشاء السيشن.' : 'Session created.'); el.reset() } catch (e) { fail(e) }
  }
  async function uploadSpeakerImage(speaker: Speaker, file: File) { const ext = file.name.split('.').pop()?.toLowerCase() || 'jpg'; const path = `${speaker.id}/photo.${ext}`; try { const { error: up } = await supabase.storage.from('speaker-images').upload(path, file, { upsert: true }); if (up) throw up; const { error } = await supabase.from('speakers').update({ image_path: path }).eq('id', speaker.id); if (error) throw error; success(ar ? 'تم تحديث صورة المتحدث.' : 'Speaker photo updated.'); await load() } catch (e) { fail(e) } }
  async function uploadSessionCover(session: Session, file: File) { const ext = file.name.split('.').pop()?.toLowerCase() || 'jpg'; const path = `${session.id}/cover.${ext}`; try { const { error: up } = await supabase.storage.from('session-covers').upload(path, file, { upsert: true }); if (up) throw up; const { error } = await supabase.from('sessions').update({ cover_path: path }).eq('id', session.id); if (error) throw error; success(ar ? 'تم تحديث غلاف السيشن.' : 'Session cover updated.'); await load() } catch (e) { fail(e) } }
  async function uploadSessionResource(session: Session, file: File) { const safeName = file.name.replace(/[^a-zA-Z0-9._-]/g, '-'); const path = `${session.id}/${Date.now()}-${safeName}`; try { const { error: up } = await supabase.storage.from('session-resources').upload(path, file); if (up) throw up; const { error } = await supabase.from('session_resources').insert({ session_id: session.id, title: file.name, file_path: path }); if (error) { await supabase.storage.from('session-resources').remove([path]); throw error } success(ar ? 'تم رفع ملف السيشن.' : 'Session resource uploaded.') } catch (e) { fail(e) } }
  async function addSessionVideo(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const videoId = extractYouTubeVideoId(videoUrl); if (!videoSessionId || !videoTitle.trim() || !videoId) { fail(new Error(ar ? 'اختاري السيشن واكتبي عنوانًا والصقي رابط YouTube صالحًا.' : 'Choose a session, add a title, and paste a valid YouTube URL.')); return } const nextPosition = videos.filter(v => v.session_id === videoSessionId).length; try { await run(() => supabase.from('session_videos').insert({ session_id: videoSessionId, title: videoTitle.trim(), youtube_video_id: videoId, position: nextPosition }), ar ? 'تمت إضافة التسجيل.' : 'Recording added.'); setVideoTitle(''); setVideoUrl('') } catch (e) { fail(e) } }
  function sendNotification(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const el = event.currentTarget; const f = new FormData(el); const title = String(f.get('title') || '').trim(); const body = String(f.get('body') || '').trim(); const url = String(f.get('url') || '').trim() || '/'; if (!title || !body) return; ask({ title: ar ? 'إرسال الإشعار؟' : 'Send notification?', description: ar ? 'سيتم إرسال هذا الإشعار للمستخدمين المستهدفين.' : 'This notification will be sent to the targeted users.', confirmLabel: ar ? 'إرسال الإشعار' : 'Send notification', tone: 'primary', action: async () => { const { error } = await supabase.functions.invoke('send-session-notification', { body: { title, body, url } }); if (error) throw error; success(ar ? 'تم إرسال الإشعار.' : 'Notification sent.'); el.reset() } }) }

  async function saveEdit(target: EditTarget, values: Record<string, unknown>) {
    setEditorBusy(true)
    try {
      let error: unknown = null
      switch (target.type) {
        case 'category': ({ error } = await supabase.from('categories').update(values as Partial<Category>).eq('id', target.item.id)); break
        case 'speaker': ({ error } = await supabase.from('speakers').update(values as Partial<Speaker>).eq('id', target.item.id)); break
        case 'series': ({ error } = await supabase.from('session_series').update(values as Partial<SessionSeries>).eq('id', target.item.id)); break
        case 'session': ({ error } = await supabase.from('sessions').update(values as Partial<Session>).eq('id', target.item.id)); break
        case 'video': ({ error } = await supabase.from('session_videos').update(values as Partial<SessionVideo>).eq('id', target.item.id)); break
      }
      if (error) throw error
      success(ar ? 'تم حفظ التغييرات.' : 'Changes saved.'); setEditing(null); await load()
    } catch (e) { fail(e); throw e } finally { setEditorBusy(false) }
  }
  function confirmDelete(kind: string, name: string, action: () => Promise<void>) { ask({ title: ar ? `حذف ${kind}؟` : `Delete ${kind}?`, description: ar ? `سيتم حذف «${name}» نهائيًا. هذا الإجراء لا يمكن التراجع عنه.` : `“${name}” will be permanently deleted. This action cannot be undone.`, confirmLabel: ar ? 'نعم، حذف' : 'Yes, delete', tone: 'danger', action }) }
  function manageUser(userId: string, action: 'set_role' | 'ban' | 'unban', role?: 'admin' | 'student') {
    const account = managedUsers.find(u => u.id === userId); const label = account?.full_name || account?.email || (ar ? 'المستخدم' : 'user')
    const title = action === 'set_role' ? role === 'admin' ? (ar ? 'ترقية إلى Admin؟' : 'Promote to Admin?') : (ar ? 'سحب صلاحية Admin؟' : 'Remove Admin access?') : action === 'ban' ? (ar ? 'تعطيل الحساب؟' : 'Disable account?') : (ar ? 'إعادة تفعيل الحساب؟' : 'Enable account?')
    const description = action === 'set_role' ? role === 'admin' ? (ar ? `سيحصل ${label} على صلاحيات الإدارة. تأكدي من هويته قبل المتابعة.` : `${label} will receive admin permissions. Verify their identity before continuing.`) : (ar ? `سيعود ${label} إلى صلاحيات الطالب العادية.` : `${label} will return to standard student access.`) : action === 'ban' ? (ar ? `لن يتمكن ${label} من تسجيل الدخول حتى إعادة تفعيل الحساب.` : `${label} will not be able to sign in until the account is enabled again.`) : (ar ? `سيتمكن ${label} من تسجيل الدخول مرة أخرى.` : `${label} will be able to sign in again.`)
    ask({ title, description, confirmLabel: title.replace('؟', ''), tone: action === 'ban' ? 'danger' : action === 'set_role' ? 'warning' : 'primary', action: async () => { setBusyUsers(true); try { const { data, error } = await supabase.functions.invoke('manage-users', { body: { action, user_id: userId, role } }); if (error) throw error; const payload = data as { error?: string }; if (payload.error) throw new Error(payload.error); success(ar ? 'تم تحديث صلاحية المستخدم.' : 'User access updated.'); await loadManagedUsers() } finally { setBusyUsers(false) } } })
  }

  const analytics = useMemo(() => {
    const attended = registrations.filter(r => r.attendance_status === 'attended').length; const avgRating = feedbackRows.length ? feedbackRows.reduce((s, r) => s + r.rating, 0) / feedbackRows.length : 0; const conversion = viewRows.length ? Math.min(100, registrations.length / viewRows.length * 100) : 0
    const categoryCounts = new Map<string, number>(), speakerCounts = new Map<string, number>(), activity = new Map<string, number>()
    for (const row of registrations) { if (row.session?.category_id) categoryCounts.set(row.session.category_id, (categoryCounts.get(row.session.category_id) ?? 0) + 1); if (row.session?.speaker_id) speakerCounts.set(row.session.speaker_id, (speakerCounts.get(row.session.speaker_id) ?? 0) + 1); activity.set(row.user_id, (activity.get(row.user_id) ?? 0) + 3) }
    for (const row of feedbackRows) activity.set(row.user_id, (activity.get(row.user_id) ?? 0) + 2); for (const row of progressRows) activity.set(row.user_id, (activity.get(row.user_id) ?? 0) + 1)
    return { attended, avgRating, conversion, categories: rankedEntries(categoryCounts, new Map(categories.map(i => [i.id, i.name]))), speakers: rankedEntries(speakerCounts, new Map(speakers.map(i => [i.id, i.name]))), students: rankedEntries(activity, new Map(profiles.map(i => [i.id, i.full_name]))) }
  }, [registrations, feedbackRows, viewRows, progressRows, categories, speakers, profiles])
  const maxRanking = Math.max(1, ...analytics.categories.map(i => i.value), ...analytics.speakers.map(i => i.value))
  const filteredUsers = useMemo(() => { const q = userSearch.trim().toLowerCase(); if (!q) return managedUsers; return managedUsers.filter(a => [a.full_name, a.email, a.phone, a.profile?.university, a.profile?.department, a.profile?.level].some(v => String(v ?? '').toLowerCase().includes(q))) }, [managedUsers, userSearch])
  const selectedUser = managedUsers.find(a => a.id === selectedUserId) ?? null
  const userAvatar = selectedUser?.profile?.avatar_path ? publicStorageUrl('profile-images', selectedUser.profile.avatar_path) : null

  return <section className="admin-page-v2">
    <div className="section-heading"><div><div className="eyebrow">{t('admin.eyebrow')}</div><h1>{t('admin.title')}</h1><p>{t('admin.subtitle')}</p></div></div>
    <div className="stats-grid stats-grid-v2"><StatCard label={t('admin.users')} value={profiles.length} /><StatCard label={t('admin.sessions')} value={sessions.length} /><StatCard label={t('admin.registrations')} value={registrations.length} /><StatCard label={t('admin.speakers')} value={speakers.length} /></div>

    <section className="panel section-gap admin-section"><div className="admin-v3-section-head"><div><span className="eyebrow">{t('admin.analytics')}</span><h2>{t('admin.analytics')}</h2></div><Icon name="chart" /></div>
      <div className="analytics-kpis"><div><span>{t('admin.views')}</span><strong>{viewRows.length}</strong></div><div><span>{t('admin.attendanceRate')}</span><strong>{registrations.length ? Math.round(analytics.attended / registrations.length * 100) : 0}%</strong></div><div className="rating-kpi"><span>{t('admin.avgRating')}</span><strong><StarRating value={Math.round(analytics.avgRating)} readOnly /></strong></div><div><span>{t('admin.videoStarts')}</span><strong>{progressRows.length}</strong></div><div><span>{t('admin.conversion')}</span><strong>{Math.round(analytics.conversion)}%</strong></div></div>
      <div className="analytics-grid"><div><h3>{t('admin.topCategories')}</h3>{analytics.categories.map(i => <div className="ranking-row" key={i.id}><span>{i.label}</span><i><b style={{ width: `${i.value / maxRanking * 100}%` }} /></i><strong>{i.value}</strong></div>)}</div><div><h3>{t('admin.topSpeakers')}</h3>{analytics.speakers.map(i => <div className="ranking-row" key={i.id}><span>{i.label}</span><i><b style={{ width: `${i.value / maxRanking * 100}%` }} /></i><strong>{i.value}</strong></div>)}</div><div><h3>{t('admin.activeStudents')}</h3>{analytics.students.map((i, n) => <div className="student-rank" key={i.id}><em>{n + 1}</em><span>{i.label}</span><strong>{i.value}</strong></div>)}</div></div>
    </section>

    {isSuperAdmin && <section className="panel section-gap admin-section"><div className="admin-v3-section-head"><div><span className="eyebrow">{t('admin.superAdmin')}</span><h2>{t('admin.userManagement')}</h2><p>{t('admin.superOnly')}</p></div><Icon name="users" /></div>
      <div className="user-directory-toolbar"><FormField label={ar ? 'ابحثي في المستخدمين' : 'Search users'}><input value={userSearch} onChange={e => setUserSearch(e.target.value)} placeholder={ar ? 'الاسم، البريد، الجامعة أو القسم' : 'Name, email, university or department'} /></FormField><span className="user-result-count">{filteredUsers.length} {ar ? 'حساب' : 'accounts'}</span></div>
      <div className="user-management-list">{filteredUsers.map(account => { const banned = Boolean(account.banned_until && new Date(account.banned_until).getTime() > Date.now()); return <article className="user-management-row" key={account.id}><div className="user-identity"><span className="sidebar-user-avatar">{(account.full_name || account.email || 'U').slice(0, 1).toUpperCase()}</span><span><strong>{account.full_name || account.email}</strong><small>{account.email}</small></span></div><div className="user-role"><span className={`role-pill ${account.super_admin ? 'super' : account.role}`}>{account.super_admin ? t('admin.superAdmin') : account.role === 'admin' ? t('admin.roleAdmin') : t('admin.roleStudent')}</span>{banned && <span className="role-pill banned">{ar ? 'معطّل' : 'Disabled'}</span>}</div><div className="user-activity"><small>{t('admin.activity')}</small><span>{account.activity.registrations} · {account.activity.feedback} · {account.activity.video_progress}</span></div><div className="user-management-actions"><button className="button user-action user-action-view" onClick={() => setSelectedUserId(account.id)}>{ar ? 'عرض التفاصيل' : 'View details'}</button>{!account.super_admin && <>{account.role === 'admin' ? <button className="button user-action user-action-demote" disabled={busyUsers} onClick={() => manageUser(account.id, 'set_role', 'student')}><Icon name="shield" />{t('admin.demote')}</button> : <button className="button user-action user-action-promote" disabled={busyUsers} onClick={() => manageUser(account.id, 'set_role', 'admin')}><Icon name="shield" />{t('admin.promote')}</button>}{banned ? <button className="button user-action user-action-enable" disabled={busyUsers} onClick={() => manageUser(account.id, 'unban')}>{t('admin.enable')}</button> : <button className="button user-action user-action-disable" disabled={busyUsers} onClick={() => manageUser(account.id, 'ban')}>{t('admin.disable')}</button>}</>}</div></article> })}{busyUsers && !managedUsers.length && <div className="page-state">{t('common.loading')}</div>}</div>
    </section>}

    <section className="panel section-gap admin-section"><div className="admin-v3-section-head"><div><h2>{t('admin.series')}</h2><p>{ar ? 'أنشئي مسارات مترابطة ورتبي السيشنات داخلها.' : 'Create structured learning series.'}</p></div><Icon name="layers" /></div><form className="admin-form-grid" onSubmit={e => void addSeries(e)}><FormField label={t('admin.seriesTitle')}><input name="title" required /></FormField><FormField label={t('admin.seriesDescription')}><input name="description" /></FormField><button className="button button-primary">{t('admin.createSeries')}</button></form><div className="admin-v3-list">{series.map(item => <div className="admin-v3-item" key={item.id}><span className="admin-v3-item-copy"><strong>{item.title}</strong><small>{item.description || '—'}</small></span><div className="admin-v3-actions"><button className="button button-ghost" onClick={() => setEditing({ type: 'series', item })}>{t('common.edit')}</button><button className="button danger" onClick={() => confirmDelete(ar ? 'السلسلة' : 'series', item.title, async () => { await run(() => supabase.from('session_series').delete().eq('id', item.id), ar ? 'تم حذف السلسلة.' : 'Series deleted.') })}>{t('common.delete')}</button></div></div>)}</div></section>

    <div className="admin-grid section-gap"><section className="panel"><h2>{t('admin.category')}</h2><form className="admin-form-grid" onSubmit={e => void addCategory(e)}><FormField label={t('admin.name')}><input name="name" required /></FormField><FormField label="Slug" hint={ar ? 'مثال: web-development' : 'Example: web-development'}><input name="slug" required /></FormField><FormField label={t('admin.description')} wide><textarea name="description" rows={3} /></FormField><button className="button button-primary">{t('common.add')}</button></form><div className="admin-v3-list">{categories.map(item => <div className="admin-v3-item" key={item.id}><span className="admin-v3-item-copy"><strong>{item.name}</strong><small>{item.slug}</small></span><div className="admin-v3-actions"><button className="button button-ghost" onClick={() => setEditing({ type: 'category', item })}>{t('common.edit')}</button><button className="button danger" onClick={() => confirmDelete(ar ? 'التصنيف' : 'category', item.name, async () => { await run(() => supabase.from('categories').delete().eq('id', item.id), ar ? 'تم حذف التصنيف.' : 'Category deleted.') })}>{t('common.delete')}</button></div></div>)}</div></section>
      <section className="panel"><h2>{t('admin.speakers')}</h2><form className="admin-form-grid" onSubmit={e => void addSpeaker(e)}><FormField label={t('admin.name')}><input name="name" required /></FormField><FormField label={ar ? 'الجامعة / الشركة / المؤسسة' : t('admin.organization')}><input name="organization" /></FormField><FormField label={ar ? 'النبذة' : 'Bio'} wide><textarea name="bio" rows={3} /></FormField><button className="button button-primary">{t('common.add')}</button></form><div className="admin-v3-list">{speakers.map(item => <div className="admin-v3-item" key={item.id}><span className="admin-v3-item-copy"><strong>{item.name}</strong><small>{item.organization || '—'}</small></span><div className="admin-v3-actions"><label className="file-action">{ar ? 'صورة' : 'Photo'}<input type="file" accept="image/*" onChange={e => e.target.files?.[0] && void uploadSpeakerImage(item, e.target.files[0])} /></label><button className="button button-ghost" onClick={() => setEditing({ type: 'speaker', item })}>{t('common.edit')}</button><button className="button danger" onClick={() => confirmDelete(ar ? 'المتحدث' : 'speaker', item.name, async () => { await run(() => supabase.from('speakers').delete().eq('id', item.id), ar ? 'تم حذف المتحدث.' : 'Speaker deleted.') })}>{t('common.delete')}</button></div></div>)}</div></section></div>

    <section className="panel section-gap admin-section"><div className="admin-v3-section-head"><div><h2>{t('admin.sessions')}</h2><p>{ar ? 'كل بيانات السيشن قابلة للتعديل من زر تعديل.' : 'Every session field can be edited from Edit.'}</p></div></div><form className="admin-form-grid" onSubmit={e => void addSession(e)}><FormField label={t('admin.titleField')}><input name="title" required /></FormField><FormField label="Slug"><input name="slug" required /></FormField><FormField label={t('admin.description')} wide><textarea name="description" rows={4} required /></FormField><FormField label={ar ? 'وقت البداية' : 'Start time'}><input name="starts_at" type="datetime-local" required /></FormField><FormField label={ar ? 'وقت النهاية' : 'End time'}><input name="ends_at" type="datetime-local" /></FormField><FormField label={ar ? 'المكان / رابط الحضور' : 'Location / meeting link'}><input name="location" /></FormField><FormField label={t('details.capacity')}><input name="capacity" type="number" min="1" defaultValue="30" required /></FormField><FormField label={ar ? 'التصنيف' : 'Category'}><select name="category_id"><option value="">{t('admin.noCategory')}</option>{categories.map(i => <option key={i.id} value={i.id}>{i.name}</option>)}</select></FormField><FormField label={ar ? 'المتحدث' : 'Speaker'}><select name="speaker_id"><option value="">{t('admin.noSpeaker')}</option>{speakers.map(i => <option key={i.id} value={i.id}>{i.name}</option>)}</select></FormField><FormField label={t('admin.series')}><select name="series_id"><option value="">{t('admin.noSeries')}</option>{series.map(i => <option key={i.id} value={i.id}>{i.title}</option>)}</select></FormField><FormField label={t('admin.seriesPosition')}><input name="series_position" type="number" min="1" defaultValue="1" /></FormField><FormField label={ar ? 'الحالة' : 'Status'}><select name="status" defaultValue="published"><option value="draft">{ar ? 'مسودة' : 'Draft'}</option><option value="published">{ar ? 'منشور' : 'Published'}</option><option value="cancelled">{ar ? 'ملغي' : 'Cancelled'}</option></select></FormField><button className="button button-primary">{t('admin.createSession')}</button></form>
      <div className="admin-v3-list">{sessions.map(item => <div className="admin-v3-item" key={item.id}><span className="admin-v3-item-copy"><strong>{item.title}</strong><small>{new Intl.DateTimeFormat(locale, { dateStyle: 'medium', timeStyle: 'short' }).format(new Date(item.starts_at))} · {item.status}</small></span><div className="admin-v3-actions"><label className="file-action">{t('admin.cover')}<input type="file" accept="image/*" onChange={e => e.target.files?.[0] && void uploadSessionCover(item, e.target.files[0])} /></label><label className="file-action">{t('admin.resource')}<input type="file" onChange={e => e.target.files?.[0] && void uploadSessionResource(item, e.target.files[0])} /></label><button className="button button-ghost" onClick={() => setEditing({ type: 'session', item })}>{t('common.edit')}</button><button className="button danger" onClick={() => confirmDelete(ar ? 'السيشن' : 'session', item.title, async () => { await run(() => supabase.from('sessions').delete().eq('id', item.id), ar ? 'تم حذف السيشن.' : 'Session deleted.') })}>{t('common.delete')}</button></div></div>)}</div></section>

    <section className="panel section-gap video-admin-panel"><div className="video-admin-heading"><div><div className="eyebrow">YouTube</div><h2>{t('admin.youtube')}</h2><p>{t('admin.youtubeHint')}</p></div></div><div className="video-admin-layout"><form className="video-admin-form" onSubmit={e => void addSessionVideo(e)}><FormField label={t('admin.videoSession')}><select value={videoSessionId} onChange={e => setVideoSessionId(e.target.value)} required><option value="">{t('admin.videoSession')}</option>{sessions.map(s => <option key={s.id} value={s.id}>{s.title}</option>)}</select></FormField><FormField label={t('admin.videoTitle')}><input value={videoTitle} onChange={e => setVideoTitle(e.target.value)} required /></FormField><FormField label={t('admin.youtubeUrl')} wide><input value={videoUrl} onChange={e => setVideoUrl(e.target.value)} inputMode="url" placeholder="https://youtu.be/..." required /></FormField><button className="button button-primary wide">{t('admin.addRecording')}</button></form><div className="video-preview">{extractYouTubeVideoId(videoUrl) ? <YouTubePlayer videoId={extractYouTubeVideoId(videoUrl)!} title={videoTitle || t('admin.preview')} /> : <div className="video-preview-empty"><strong>{t('admin.preview')}</strong><span>{ar ? 'الصقي رابط YouTube صالحًا لرؤية المعاينة.' : 'Paste a valid YouTube URL to preview it.'}</span></div>}</div></div><div className="admin-v3-list">{videos.map(item => <div className="admin-v3-item" key={item.id}><span className="admin-v3-item-copy"><strong>{item.title}</strong><small>{sessions.find(s => s.id === item.session_id)?.title || '—'}</small></span><div className="admin-v3-actions"><button className="button button-ghost" onClick={() => setEditing({ type: 'video', item })}>{t('common.edit')}</button><button className="button danger" onClick={() => confirmDelete(ar ? 'التسجيل' : 'recording', item.title, async () => { await run(() => supabase.from('session_videos').delete().eq('id', item.id), ar ? 'تم حذف التسجيل.' : 'Recording deleted.') })}>{t('common.delete')}</button></div></div>)}</div></section>

    <section className="panel section-gap notification-admin-panel"><h2>{t('admin.push')}</h2><form className="notification-form" onSubmit={sendNotification}><FormField label={t('admin.pushTitle')}><input name="title" required /></FormField><FormField label={t('admin.pushBody')}><textarea name="body" required /></FormField><FormField label={ar ? 'الرابط داخل المنصة' : 'App link'}><input name="url" defaultValue="/" /></FormField><button className="button button-primary">{t('admin.sendPush')}</button></form></section>

    {selectedUser && <div className="user-detail-backdrop" role="presentation" onMouseDown={e => { if (e.currentTarget === e.target) setSelectedUserId(null) }}><aside className="user-detail-drawer" role="dialog" aria-modal="true"><header className="user-detail-head"><div className="user-detail-identity">{userAvatar ? <img className="user-detail-avatar" src={userAvatar} alt="" /> : <span className="user-detail-avatar">{(selectedUser.full_name || selectedUser.email).slice(0, 1).toUpperCase()}</span>}<div><h2>{selectedUser.full_name || selectedUser.email}</h2><p>{selectedUser.email}</p></div></div><button className="user-detail-close" onClick={() => setSelectedUserId(null)}><Icon name="close" /></button></header><section className="user-detail-section"><h3>{ar ? 'بيانات الحساب' : 'Account details'}</h3><div className="user-detail-grid"><div className="user-data-cell"><span>Email</span><strong>{selectedUser.email}</strong></div><div className="user-data-cell"><span>{ar ? 'تأكيد البريد' : 'Email verified'}</span><strong>{selectedUser.email_confirmed_at ? (ar ? 'مؤكد' : 'Verified') : (ar ? 'غير مؤكد' : 'Not verified')}</strong></div><div className="user-data-cell"><span>{ar ? 'الهاتف' : 'Phone'}</span><strong>{selectedUser.phone || '—'}</strong></div><div className="user-data-cell"><span>{ar ? 'الدور' : 'Role'}</span><strong>{selectedUser.super_admin ? 'Super Admin' : selectedUser.role}</strong></div><div className="user-data-cell wide"><span>User ID</span><strong>{selectedUser.id}</strong></div><div className="user-data-cell"><span>{ar ? 'إنشاء الحساب' : 'Created'}</span><strong>{new Intl.DateTimeFormat(locale, { dateStyle: 'medium' }).format(new Date(selectedUser.created_at))}</strong></div><div className="user-data-cell"><span>{t('admin.lastSignIn')}</span><strong>{selectedUser.last_sign_in_at ? new Intl.DateTimeFormat(locale, { dateStyle: 'medium', timeStyle: 'short' }).format(new Date(selectedUser.last_sign_in_at)) : '—'}</strong></div></div></section><section className="user-detail-section"><h3>{ar ? 'البروفايل' : 'Profile'}</h3><div className="user-detail-grid"><div className="user-data-cell"><span>{ar ? 'الاسم' : 'Name'}</span><strong>{selectedUser.profile?.full_name || selectedUser.full_name || '—'}</strong></div><div className="user-data-cell"><span>{ar ? 'الجامعة' : 'University'}</span><strong>{selectedUser.profile?.university || '—'}</strong></div><div className="user-data-cell"><span>{ar ? 'القسم' : 'Department'}</span><strong>{selectedUser.profile?.department || '—'}</strong></div><div className="user-data-cell"><span>{ar ? 'المستوى' : 'Level'}</span><strong>{selectedUser.profile?.level || '—'}</strong></div><div className="user-data-cell wide"><span>{ar ? 'النبذة' : 'Bio'}</span><p>{selectedUser.profile?.bio || '—'}</p></div></div></section><section className="user-detail-section"><h3>{t('admin.activity')}</h3><div className="user-detail-grid"><div className="user-data-cell"><span>{t('admin.registrations')}</span><strong>{selectedUser.activity.registrations}</strong></div><div className="user-data-cell"><span>{ar ? 'التقييمات' : 'Feedback'}</span><strong>{selectedUser.activity.feedback}</strong></div><div className="user-data-cell"><span>{ar ? 'الفيديوهات' : 'Video progress'}</span><strong>{selectedUser.activity.video_progress}</strong></div></div></section></aside></div>}

    <AdminEditorDialog target={editing} categories={categories} speakers={speakers} series={series} sessions={sessions} language={language} busy={editorBusy} onClose={() => !editorBusy && setEditing(null)} onSave={saveEdit} />
    <ConfirmDialog open={Boolean(confirmation)} title={confirmation?.title || ''} description={confirmation?.description || ''} confirmLabel={confirmation?.confirmLabel || ''} cancelLabel={ar ? 'إلغاء' : 'Cancel'} tone={confirmation?.tone || 'primary'} busy={confirmBusy} onCancel={() => !confirmBusy && setConfirmation(null)} onConfirm={() => void executeConfirmation()} />
  </section>
}
